
For build and run using compose: 

`docker compose up --build`

show compose stack containers:

`docker compose ps`

show compose stack container logs:

`docker compose logs`

OR

`docker compose logs {service_name}`

To stop and remove compose stack

`docker compose down`